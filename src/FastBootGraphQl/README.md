# GraphCommerce_FastBootGraphQl

The work a GraphQL request repeats on php-fpm, remembered: the stitched schema as a PHP array, built-in scalars answered without a walk over every declared type, a query parsed once and validated once per opcache version, the guest tax factor of the response cache id and the placeholder image URL from the cache. The repository README holds the design, the numbers and the rollout.
